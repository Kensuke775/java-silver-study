## 目次

**問題一覧**

- [問題1](#q1)
- [問題2](#q2)
- [問題3](#q3)
- [問題4](#q4)
- [問題5](#q5)
- [問題6](#q6)
- [問題7](#q7)
- [問題8](#q8)
- [問題9](#q9)
- [問題10](#q10)
- [問題11](#q11)
- [問題12](#q12)
- [問題13](#q13)
- [参考：javadocの使い方](#ref-javadoc)

<a id="q1"></a>
## 問題1

**要点**

- Javaは元々Sun Microsystems社が開発（1996年1月リリース）。2010年にOracle社が買収
- 現在の仕様策定はOracle単独ではなく、JCP（Java Community Process）というコミュニティが行っている
- 「プログラミング言語としてのJava」と「プラットフォームとしてのJava」の2つの側面がある
- 実行環境はJVMが提供する
- 仕様はJava SE／Java EE／Java MEの3エディション

以下の説明のうち、正しいものはどれですか。（3つ選択）

A. Javaは元々Sun Microsystems社によって開発され、後にOracle社が買収した
B. 現在、Javaの仕様はOracle社が単独で決定している
C. JVMは、コンパイル済みのバイトコードを実行するための実行環境を提供する
D. Java SE、Java EE、Java MEという3つのエディションがある
E. Javaは「プログラミング言語」としての側面のみを持ち、プラットフォームとしての側面は持たない

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, C, D
正解：A, C, D
迷ったポイント：なし（正解後、JVMと「プラットフォーム」の関係について確認質問あり——JVMが提供する実行環境自体はOS非依存だが、JVMの実装（バイナリ）自体はOSごとに別物である点を再確認）

---

<a id="q2"></a>
## 問題2

**要点**

- Javaはオブジェクト指向というプログラミングパラダイムを採用
- 定義：「データと、そのデータを処理するコードのみをクラスにまとめ、他の必要なクラスと相互作用をすることで全体の処理を進める」

Javaが採用しているプログラミングパラダイムの説明として、正しいものはどれですか。（1つ選択）

A. データとそのデータを処理するコードを別々に管理し、処理の実行順序を細かく制御することを中心とする
B. データとそのデータを処理するコードをクラスとしてまとめ、他の必要なクラスと相互作用することで全体の処理を進める
C. データとそのデータを処理するコードをクラスとしてまとめるが、他のクラスとは一切相互作用せず、常に単独で処理を完結させる
D. 関数を組み合わせることを中心とし、副作用を持たないことを重視する
E. すべての処理を1つの巨大なメソッドにまとめ、クラス分割を避けることを重視する

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：B
正解：B
迷ったポイント：なし

---

<a id="q3"></a>
## 問題3

**要点**

- コンパイルされたバイトコードがJVM上で動く
- バイトコードは異なる環境（Windows/macOS/Linux）でもそのまま実行できる（プラットフォーム非依存）
- ただしJVM自体はプラットフォームに適したものが別々に必要（Windows用/macOS用/Linux用は別物）
- 32bit/64bitはCPU/OSのアーキテクチャの話で、バイトコードとは別軸の概念（混同注意）
- `.class`ファイル先頭の`0xCAFEBABE`はclassファイルであることを示すマジックナンバー
- 「バイトコード」の語源は8ビット（1バイト）単位でJVMの命令がエンコードされていることから

以下の説明のうち、正しいものはどれですか。（3つ選択）

A. コンパイルされたバイトコードは、JVM自体を含めどの環境でもそのまま同じように動作する
B. ソースプログラムをコンパイルしたバイトコードは、Windows/macOS/Linuxなど異なる環境でもそのまま実行できる
C. JVM自体は、実行するOSに適したものを別々に用意する必要がある
D. 32bit版と64bit版の違いは、バイトコードそのものの仕様が変わることを意味する
E. `.class`ファイルの先頭には、そのファイルがclassファイルであることを示すマジックナンバーが記録されている
F. 「バイトコード」という名称は、JVMの命令が2バイト単位でエンコードされていることに由来する

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：B, C, E
正解：B, C, E
迷ったポイント：なし

---

<a id="q4"></a>
## 問題4

**要点**

- JRE（Java Runtime Environment）＝ JVM ＋ クラスライブラリ
- JDK（Java SE Development Kit）＝ JRE ＋ 開発用ツール（javac, java等）
- 包含関係：JDK ⊃ JRE ⊃ JVM
- Oracle社が提供する2つのJDK：Oracle JDK（ライセンス: Oracle No-Fee Terms and Conditions、個人利用無料）／OpenJDK（Oracleビルド、ライセンス: GNU GPL v2.0）

以下の説明のうち、正しいものはどれですか。（3つ選択）

A. JRE（Java Runtime Environment）は、JVMとクラスライブラリで構成される
B. JDK（Java SE Development Kit）は、JVMのみで構成され、クラスライブラリは含まれない
C. JDKはJREを包含し、JREはJVMを包含するという関係になっている（JDK ⊃ JRE ⊃ JVM）
D. Oracle社が提供するJDKには、Oracle JDKとOpenJDKの2種類がある
E. Oracle JDKはGNU GPL v2.0ライセンスで提供されており、個人利用は有料である
F. OpenJDKは「Oracle No-Fee Terms and Conditions」ライセンスで提供されており、個人利用は無料である

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, C, D
正解：A, C, D
迷ったポイント：なし

---

<a id="q5"></a>
## 問題5

**要点**

- `javac`（java + compiler）：`.java`（ソース）→`.class`（バイトコード）に変換するコンパイラ
- `java`：コンパイル済みの`.class`をJVM上で実行するコマンド
- `javac`と`java`は別コマンド（javacがjavaを内部で呼んでいる、という関係ではない）
- 重要な認識：`java ClassName`（通常実行）は、ディスク上の既存の`.class`をそのまま実行するだけで、ソース（`.java`）は一切見ない。ソースを編集しても、再度`javac`でコンパイルしない限り実行結果には反映されない
- 対照的に`java File.java`（ソースファイルモード）は、実行のたびにメモリ上でコンパイルし直すので、ソースの変更が毎回反映される

以下の説明のうち、正しいものはどれですか。（3つ選択）

A. `javac`は`.java`ファイルを`.class`ファイル（バイトコード）に変換するコンパイラである
B. `javac`は`java`コマンドの一種であり、内部的に`java`を呼び出している
C. `java ClassName`（通常実行）は、ディスク上に存在する既存の`.class`ファイルをそのまま実行するだけで、ソースファイル（`.java`）の内容は一切参照しない
D. ソースファイルを編集した後、再度`javac`でコンパイルしなくても、`java ClassName`を実行すれば編集内容がそのまま反映される
E. `java File.java`（ソースファイルモード）は、実行のたびにその場でメモリ上にコンパイルするため、ソースの変更が毎回反映される

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, C, E
正解：A, C, E
迷ったポイント：なし

---

<a id="q6"></a>
## 問題6

**要点**

- `main`メソッドは実行に必須。宣言は`public static void main(String[] args) {}`の形に固定
  - `public`・`static`は修飾子、`void`は戻り値なしを示す型指定
  - `args`という引数名自体は決まりではなく慣習（自由に変更可）。`String... args`という可変長引数の書き方も可
- 文（statement）の末尾は`;`が必須。`{ }`で囲まれた範囲が**ブロック**
- コメントは`//`（1行）と`/* */`（複数行）の2種類。コンパイル後の`.class`には含まれない
- `/** */`は**ドキュメンテーションコメント**（`javadoc`コマンドでHTML化できる）

以下の説明のうち、正しいものはどれですか。（3つ選択）

A. 1行コメント（`//`）や複数行コメント（`/* */`）は、コンパイル後の`.class`ファイルには一切含まれない
B. `/** */`で書かれたコメントは、`javadoc`コマンドによってHTML形式のドキュメントに変換できる
C. Javaの文（statement）は、末尾に`;`を付けなくてもコンパイルが成功する
D. `{ }`で囲まれた範囲のことを「ブロック」と呼ぶ
E. mainメソッドの引数名は必ず`args`という名前にしなければならず、他の名前に変更するとコンパイルエラーになる

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, B, D
正解：A, B, D
迷ったポイント：なし

---

<a id="q7"></a>
## 問題7

**要点**

- 基本の流れ：`javac Hello.java` → `Hello.class`生成 → `java Hello`で実行
- コンパイルエラー時、エラーメッセージは「ファイル名:行数: エラー: ...」という形式で、`^`（ハット）でエラー箇所を示す
- インデントは半角スペース/タブどちらでもよく、コンパイルに影響しない
- 大文字小文字は区別される（`Main()`と`main()`は別物）

以下の説明のうち、正しいものはどれですか。（4つ選択）

A. Javaのコードは大文字・小文字を区別する。そのため`main`と`Main`は全くの別物として扱われる
B. `public static void Main(String[] args) { ... }`（Mが大文字）と書いた場合、コンパイル自体は成功する
C. `public static void Main(String[] args) { ... }`（Mが大文字）と書いたクラスを`java`コマンドで実行すると、正しく起動してmainメソッドの中身が実行される
D. インデントに半角スペースを使うかタブを使うかは、コンパイル結果に一切影響しない
E. コンパイルエラーが発生すると、エラーメッセージには「ファイル名:行数:」という形式でエラー箇所が示される

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, B, D, E
正解：A, B, D, E
迷ったポイント：なし

---

<a id="q8"></a>
## 問題8

**要点**

- 1つのソースファイルに複数クラスを宣言可能。コンパイルすると**クラスごとに別々の`.class`ファイル**が生成される
- `public`付きクラスはソースファイル名と一致必須、かつ1ファイルに1つまで
- `main`メソッドは「1ファイルにつき1つ」という制限はない。各クラスが独立して`main`を持てる
- 別クラスの実行が失敗するのは「複数main禁止」だからではなく、単にそのクラス自身に`main`が無いだけ

以下の説明のうち、正しいものはどれですか。（3つ選択）

A. 1つのソースファイルに複数のクラスを宣言することができる
B. 1つのソースファイル内で複数クラスを宣言した場合、コンパイルすると各クラスごとに別々の`.class`ファイルが生成される
C. 1つのソースファイルにつき、`main`メソッドを持てるのは1つのクラスだけである
D. 1つのソースファイル内の各クラスは、それぞれ独立して`main`メソッドを持つことができる
E. `public`が付いていないクラスに対して`java`コマンドで実行しようとしてエラーになるのは、「複数のmainメソッドが存在するのを防ぐルール」に違反しているからである

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, B, D
正解：A, B, D
迷ったポイント：なし

---

<a id="q9"></a>
## 問題9

**要点**

- `javac`を介さず`java File.java`で直接実行できる機能（Java 11以降）。コンパイル結果はメモリ上のみで、ディスクに`.class`は残らない
- 複数クラスがある場合、ファイル内の最初のクラスだけが実行される（後続のクラスにmainがあっても無視される）
- ソースファイルモードでは、ファイル名とpublicクラス名が不一致でも実行できる（`javac`による通常コンパイルではこの不一致は常にコンパイルエラー）

以下の説明のうち、正しいものはどれですか。（3つ選択）

A. ソースファイルモード（`java File.java`）では、コンパイル結果はメモリ上にのみ保持され、ディスクに`.class`ファイルは生成されない
B. ソースファイルモードでは、ファイル内に複数クラスがある場合、後ろに書かれたクラスにmainメソッドがあればそちらが優先して実行される
C. ソースファイルモードでは、ファイル内の**最初に書かれたクラス**が実行される（そのクラスにmainが無ければ、後続のクラスにmainがあっても実行されずエラーになる）
D. ソースファイルモードでは、ファイル名とpublicクラス名が一致していなくても実行できる
E. 通常のコンパイル（`javac`）でも、ファイル名とpublicクラス名が一致していなくても実行できる

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, C, E
正解：A, C, D
迷ったポイント：Eを誤って選択（javacでも不一致を許容すると誤解）、Dを見落とした。ファイル名/publicクラス名の不一致を許容するのはソースファイルモードだけの特例で、javacでは常にコンパイルエラーになる点を混同した

---

<a id="q10"></a>
## 問題10

**要点**

- パッケージ：クラスを役割ごとに管理し、クラス名の衝突を避け、アクセス制御を柔軟にする仕組み
- 宣言は`package パッケージ名;`。ソースファイルの先頭（コメント・空白を除く）に記述し、1ファイルにつき1つのみ
- package宣言のないクラスは無名パッケージに所属する（＝すべてのクラスは必ず何らかのパッケージに属する）
- パッケージ化されたクラスは完全修飾名（FQCN）で認識される。実行時にディレクトリ構造とパッケージ宣言が一致していないと失敗する

以下の説明のうち、正しいものはどれですか。（4つ選択）

A. package宣言は、ソースファイルの先頭（コメント・空白を除く）に記述しなければならない
B. 1つのソースファイルに記述できるpackage宣言は1つだけである
C. package宣言のないクラスは、コンパイルエラーになる
D. package宣言のないクラスは「無名パッケージ」に所属する。つまりすべてのクラスは必ず何らかのパッケージに属している
E. `package com.se;`と宣言したソースファイルは、それを格納するディレクトリ構造が`com/se`になっていなくても、`javac`でのコンパイル自体は成功する
F. `package com.se;`と宣言したクラスをコンパイルした後、`java Main`（完全修飾名を使わない）で実行すると成功する

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, B, D, E
正解：A, B, D, E
迷ったポイント：なし

---

<a id="q11"></a>
## 問題11

**要点**

- `javac -d 出力先ディレクトリ ソースファイル`で、package宣言と同じディレクトリ構造を自動生成しながらコンパイルできる
- `-d`を付けない場合は、package宣言があってもディレクトリは自動生成されず、`.class`はカレントディレクトリに平置きされる
- `-d`でコンパイル後は、その出力先ディレクトリを起点に完全修飾名（`java com.se.Main`）で実行できる

以下の説明のうち、正しいものはどれですか。（3つ選択）

A. `javac -d`オプションは、コンパイル済み`.class`ファイルの出力先ディレクトリを指定するものである
B. `javac -d 出力先 ソースファイル`を実行すると、package宣言に対応するディレクトリ構造（例：`com/se/`）が出力先の下に自動生成される
C. `-d`オプションを付けずにコンパイルした場合でも、package宣言があれば自動的に`com/se/`のようなディレクトリ構造の中に`.class`ファイルが生成される
D. `-d`オプションでコンパイルした後は、そのディレクトリ構造を起点として完全修飾名（例：`java com.se.Main`）で実行できる
E. `-d`オプションは、コンパイルするソースファイル自体の場所を指定するものである

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, B, D
正解：A, B, D
迷ったポイント：なし

---

<a id="q12"></a>
## 問題12

**要点**

- 核心：`-cp`を付けなくても、Javaは常に「カレントディレクトリ」をデフォルトのクラスパスとして探している
- `-cp`と`-classpath`は同じ意味（`-cp`は省略形）
- `-cp`/`-classpath`のフラグを付け忘れると、パスの文字列がクラス名として誤解釈される
- `-cp`に絶対パスを指定すればカレントディレクトリに依存しない。相対パスだとカレントディレクトリに依存が残る
- OSのCLASSPATH環境変数もあるが、`-cp`/`-classpath`オプションが指定された場合はそちらが優先される

以下の説明のうち、正しいものはどれですか。（4つ選択）

A. `-cp`オプションを付けなくても、Javaは常に「カレントディレクトリ」をデフォルトのクラスパスとして探している
B. `-cp`と`-classpath`は同じ意味であり、`-cp`は`-classpath`の省略形である
C. `java classes com.se.Main`のように`-cp`/`-classpath`のフラグを付け忘れると、`classes`という文字列がクラス名として解釈されてしまう
D. `-cp`に絶対パスを指定した場合、実行時のカレントディレクトリに関わらず、常に同じ場所からクラスを探す
E. `-cp`に相対パスを指定した場合も、絶対パスと同様にカレントディレクトリの影響を一切受けない
F. OSのCLASSPATH環境変数が設定されている場合、`-cp`/`-classpath`オプションを指定してもCLASSPATH環境変数の方が優先される

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, B, C, D
正解：A, B, C, D
迷ったポイント：なし

---

<a id="q13"></a>
## 問題13

**要点**

- クラスが明示的にコンストラクタを1つでも定義すると、引数無しの「無料」デフォルトコンストラクタは自動生成されなくなる
- サブクラスが自分でコンストラクタを1つも定義しない場合、Javaは自動でデフォルトコンストラクタを生成し、その中身は暗黙的に親クラスの引数無しコンストラクタ`super();`を1行目に呼び出す
- 親クラスに引数無しコンストラクタが無い状態でこれが起きると、コンパイルエラーになる
- （問題8の復習）`main`は1ファイルにつき1つの制限はなく、`java ClassName`は指定したクラスだけを見る

```java
class Sample {
    int x;
    int y;

    Sample(){}

    Sample(int x, int y){
        this.x = x;
        this.y = y;
    }
    void addCount(){
        this.x += 1;
        this.y += 1;
    }
    public static void main(String[] args) {
        System.out.println("World");
    }
}

public class Main extends Sample {
    public static void main(String[] args) {
        Sample s = new Sample(2, 2);
        s.addCount();
        System.out.println("Hello" + s.x + s.y);
    }
}
```

以下の説明のうち、正しいものはどれですか。（4つ選択）

A. `Main`クラスは自分でコンストラクタを定義していないため、Javaが自動的に引数無しのデフォルトコンストラクタを生成する
B. Javaが生成する`Main`のデフォルトコンストラクタは、暗黙的に`super();`（親クラス`Sample`の引数無しコンストラクタ呼び出し）を1行目に持つ
C. もし`Sample(){}`を削除すると、`Sample(int, int)`しか無くなるため、`Main`の暗黙の`super();`が呼び出し先を見つけられずコンパイルエラーになる
D. `java Main`を実行すると、`Hello33`と出力される
E. `java Sample`を実行すると、`Main`クラスのmainメソッドが優先して実行される

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A, B, C, D
正解：A, B, C, D
迷ったポイント：なし

---

<a id="ref-javadoc"></a>
## 参考：javadocの使い方

問題6の`/** */`（ドキュメンテーションコメント）に関連して、実際に`sample/chap7/ex14/Main.java`で`javadoc`コマンドを実演確認した内容。

**何のためのものか**：javadocは「自分が今すぐ何かを調べるため」のコマンドではなく、**コードに書いた説明を、後で他の人（や未来の自分）が調べられる形（HTML）に変換して公開するため**のツール。流れは3ステップ：

1. コードを書いた人が、クラス/メソッドの直前に`/** */`で説明・`@param`・`@return`などを書いておく
2. `javadoc`コマンドを実行してHTMLに変換する（＝生成する人がやる作業はここまで）
3. **そのコードを使う別の人**が、生成されたHTMLを見て調べる（＝実際に「調べる」のはこの人）

普段目にする`String`や`ArrayList`などの公式Java APIドキュメントも、JDKのソース自体に書かれた`/** */`コメントを、Oracleが同じ`javadoc`コマンドでビルドして公開しているもの——公式ドキュメントと同じ仕組みで生成される。

**実行コマンド**

```bash
cd <対象の.javaがあるディレクトリ>
javadoc -d html Main.java
```

- `-d html`：出力先ディレクトリを指定（省略すると実行したディレクトリ直下に大量のHTMLファイルが直接生成される）
- 実行すると`html/`ディレクトリの中に`Main.html`・`index.html`等が生成される
- `open html/Main.html`（Mac）でブラウザ表示して確認できる

**対応関係の実例**（`sample/chap7/ex14/Main.java`の`add`メソッドで確認済み）

```java
/**
 * 2つの整数を加算する。
 *
 * @param a 加算する1つ目の整数
 * @param b 加算する2つ目の整数
 * @return aとbの合計値
 */
public int add(int a, int b) {
    return a + b;
}
```

上記から生成されたHTMLは、`@param`が「パラメータ：」セクション、`@return`が「戻り値：」セクションにそのまま変換される。通常の`//`・`/* */`コメントは`javadoc`の対象外（`.class`に含まれないのと同様、HTML化もされない）。
