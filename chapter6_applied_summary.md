# 6章 応用問題 要点まとめ（散歩用）

`chapter6_summary2.md`（応用問題1〜5の詳しい議論・検証コード）の早見表版。1項目1行で読める。

| # | テーマ | 要点 |
|---|---|---|
| 1 | sealedの修飾子順序 | `sealed abstract`でも`abstract sealed`でも同じ。コンパイラは修飾子の並び順を問わない |
| 2 | permits省略の条件 | 「子が1つのときだけ」ではない。**直接の子が全員、同一ソースファイル内に揃っていれば**何人いても省略可。パッケージが同じかは無関係 |
| 3 | permitsに書けるのは直接の子のみ | 孫（間接の子孫）を`permits`に書くのは無効。中間クラスが`permits`から漏れる扱いになりエラー |
| 4 | sealedの直接の子には修飾子必須 | `final`・`sealed`・`non-sealed`のいずれかを必ず明示。無指定はエラー |
| 5 | permits省略でもsealedの強制力は同じ | 省略は「同一ファイルで完結しているなら書かなくてよい」という省略記法にすぎない。部外者を拒否する強さは明示時と完全に同じ |
| 6 | extends vs implements | クラス継承は`extends`、インタフェース実装は`implements`。`sealed`の有無とは無関係で、継承元が`class`か`interface`かだけで決まる |
| 7 | フィールドの多段隠蔽 | フィールドアクセスは常に**宣言型基準**（動的束縛なし）。3階層あっても同じ |
| 8 | メソッド内の無修飾フィールド | 呼び出し元の型ではなく、**そのメソッドが定義されているクラス自身**のフィールドを指す |
| 9 | super.メソッド()の遡り範囲 | **直近の親（1階層）だけ**。祖先を連鎖して全部実行されるわけではない |
| 10 | protectedの別パッケージ・サブクラス経由アクセス | アクセスに使う式の型が、アクセスしている側のクラス自身かそのサブタイプでなければ不可。親クラス型・無関係な兄弟クラス型を経由するとNG |
| 11 | thisの正体 | 「省略可能な引数」ではない。呼び出し時にJVMが暗黙で結びつける、構文上書けない特別な参照。`this.`の省略は表記上の話 |
| 12 | package-privateのオーバーライドは別パッケージを挟むと切れる | 継承リンク1本ごとに判定。別パッケージを挟んだリンクだけが「無関係な別メソッド」化し、他の健全なリンクには影響しない |
| 13 | 切れたリンクの先で別リンクが繋がっていても復活しない | 一度切れた地点から先は新しい別系統。元の枠（vtableスロット）には戻らない |
| 14 | protected/publicなら連鎖は切れない | 子孫である限り、何階層・何パッケージ跨いでもオーバーライドの連鎖が保たれる（package-private特有の弱点） |
| 15 | protectedは子孫全員に及ぶ | 直接の子だけでなく、間に何もオーバーライドしないクラスを挟んでも、さらに下の子孫までprotectedメンバーは継承・オーバーライド可能 |
| 16 | 見えないメソッドは「無関係な別物」、見えるメソッドは「オーバーライドの試み」 | 親のメソッドが子から見えない（private/別パッケージのpackage-private）→同名でもエラーなしの別メソッド。**見える（protected以上）→必ずオーバーライドとして扱われ、アクセスを狭めるとコンパイルエラー** |
| 17 | アクセス修飾子の広さの順序 | `private < 無印(default) < protected < public`。オーバーライドは元以上の広さでなければならない（狭い元ほど選べる選択肢が絞られる） |
| 18 | フィールド初期化のタイミング | `super()`が完全に終わったあと、コンストラクタ本体の前に実行される。親のコンストラクタからオーバーライド可能メソッドを呼ぶと、子のフィールドが未初期化(デフォルト値)の状態でそのメソッドが動いてしまう |
| 19 | 代入の有無は構築順序と無関係 | `new B()`でも`B b = new B()`でも内部の実行順序は同じ。変数への代入はコンストラクタが完全に終わった最後の瞬間に起きるだけ |
| 20 | 「コンストラクタからオーバーライド可能メソッドを呼ぶな」の正確な意味 | 直接呼んでいるかは無関係。呼び出しチェーンのどこかに`private`/`static`/`final`でないメソッドが紛れていれば同じ危険がある |

---

## 一言でまとめると

- **sealed/permits**：省略は書き方の省略にすぎず、強制力は変わらない。直接の子限定、修飾子必須
- **フィールド・private・package-private(別パッケージ)**：動的束縛の対象外（宣言型基準／無関係な別物）
- **protected以上**：子孫なら何階層・何パッケージでも継承・オーバーライドが繋がる。狭めようとすると即エラー
