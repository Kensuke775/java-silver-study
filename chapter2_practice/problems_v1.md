## 目次

**問題一覧**

- [問題1](#q1)
- [問題2](#q2)
- [問題2-α](#q2a)
- [問題3](#q3)
- [問題4](#q4)
- [問題5](#q5)
- [問題6](#q6)
- [問題7](#q7)
- [問題8](#q8)
- [問題9](#q9)
- [問題10](#q10)
- [問題11](#q11)
- [参考：進数変換の計算方法](#ref-conversion)

<a id="q1"></a>
## 問題1

**要点**

- アンダースコア(`_`)は数値リテラルの可読性を上げるための区切り文字。**値自体には一切影響しない**（コンパイル時に単純に無視される）
- アンダースコアが使えない位置：①数値の先頭・末尾、②小数点の直前・直後、③`F`/`D`/`L`などサフィックスの直前、④`0x`（16進数）・`0b`（2進数）プレフィックスの途中・直後
- 上記以外（数字と数字の間）はいくつ連続しても○（`1___0`のようにアンダースコアが連続してもOK）
- **例外**：8進数の先頭`0`は`0x`/`0b`のような特別なプレフィックス扱いされない、ただの数字の1つ。そのため先頭`0`の直後にアンダースコアを置いてもエラーにならない（`0_777`はOK）

次のコードのうち、コンパイルエラーになる行はどれですか。（3つ選択）

```java
public class Main {
    public static void main(String[] args) {
        int a = 3_600;
        int b = 900_;
        int c = 0x_1F;
        double d = 9.8_1;
        int e = 0_777;
        float f = 2.5_F;
    }
}
```

A. `int a = 3_600;`
B. `int b = 900_;`
C. `int c = 0x_1F;`
D. `double d = 9.8_1;`
E. `int e = 0_777;`
F. `float f = 2.5_F;`

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：B, C, F
正解：B, C, F
迷ったポイント：なし

---

<a id="q2"></a>
## 問題2

**要点（発展編）**

- 前問の4パターン＋8進数例外に加え、以下の2点も押さえておく
- **`_56`のような「先頭アンダースコア」は、実は数値リテラルの構文エラーではない。** アンダースコアは識別子（変数名）としても使える文字なので、Javaのレキサ（字句解析）は`_56`をまるごと「1つの識別子」として読む。結果、`int b = _56;`は「`_56`という名前の変数が見つからない」というシンボル解決エラー（`シンボルを見つけられません`）になる。**「先頭アンダースコアの数値リテラルは不正」という理由でコンパイルエラーになっているわけではない**（結果的にどちらもコンパイルは通らないが、エラーの原因は別物）
- 指数表記（`e`/`E`）でも同じ4分類のルールが適用される：`e`の**直前・直後**にアンダースコアは置けないが、指数部の**数字と数字の間**（例：`12e1_0`）は通常の数字列と同じで問題ない

次のコードの各行に関する説明のうち、**誤っているもの**はどれですか。（3つ選択）

```java
public class Main {
    public static void main(String[] args) {
        int a = 4_2_0;
        int b = _56;
        long c = 78_;
        double d = 5_.5;
        double e = 5._5;
        long f = 100_L;
        int g = 0b_110;
        int h = 0_x1A;
        int i = 0_53;
        double j = 12e1_0;
    }
}
```

A. `int a = 4_2_0;` は、数字と数字の間であればアンダースコアを連続して複数個置いても問題なくコンパイルできる
B. `int b = _56;` は、数値リテラルの先頭にアンダースコアを置くルール違反として、コンパイルエラーになる
C. `long c = 78_;` は、末尾にアンダースコアがあるため、「不正なアンダースコアです」というコンパイルエラーになる
D. `double d = 5_.5;` は、小数点の直前にアンダースコアがあるため、コンパイルエラーになる
E. `double e = 5._5;` は、小数点の直後であっても、数字と数字の間と同様にアンダースコアが許可されるため、問題なくコンパイルできる
F. `long f = 100_L;` は、サフィックス`L`の直前にアンダースコアを置くことができないために生じるコンパイルエラーである
G. `int g = 0b_110;` は、2進数プレフィックス`0b`の直後にアンダースコアを置いているため、コンパイルエラーになる
H. `int h = 0_x1A;` は、`0x`というプレフィックスの途中にアンダースコアを割り込ませた書き方であり、これも「不正なアンダースコアです」というコンパイルエラーになる
I. `int i = 0_53;` は、先頭の`0`が`0x`や`0b`と同じ「特別なプレフィックス」として扱われるため、直後にアンダースコアを置くとコンパイルエラーになる
J. `double j = 12e1_0;` は、アンダースコアが指数部の数字と数字の間に置かれているだけであり、`e`の直前・直後ではないため、コンパイルエラーにはならない

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：E, I, J
正解：B, E, I
迷ったポイント：Bを見落とし、代わりにJを誤って選択。Jは指数部の数字間のアンダースコアで正しい説明（eの直前直後ではないのでOK）。Bは「先頭アンダースコアの数値リテラルは不正」という理由づけ自体が誤りで、実際は`_56`が識別子として読まれてのシンボル解決エラーという点を見抜けなかった。

---

<a id="q2a"></a>
## 問題2-α

**要点**

- 問題2と同じ4つの狙い（識別子と誤認される先頭アンダースコア／小数点まわりの位置精度／サフィックス直前の文字が本当にアンダースコアかどうか／8進数の先頭0の例外）を、別の数値で出題した派生問題
- 新しい着眼点：「小数点の**直後**」「サフィックスの**直前**」という言葉は、あくまで**隣接する1文字**を指す。1文字でも離れて数字が挟まっていれば、それは「数字と数字の間」ルールが適用され、問題なくコンパイルできる

次のコードの各行に関する説明のうち、**誤っているもの**はどれですか。（4つ選択）

```java
public class Main {
    public static void main(String[] args) {
        int a = 8_1_5;
        int b = _29;
        long c = 500_;
        double d = 6_.02;
        double e = 6.0_2;
        long f = 200_0L;
        int g = 0x_4D;
        int h = 0_x7B;
        int i = 0_46;
        double j = 9e_12;
    }
}
```

A. `int a = 8_1_5;` は、数字と数字の間であればアンダースコアを連続して複数個置いても問題なくコンパイルできる
B. `int b = _29;` は、数値リテラルの先頭にアンダースコアを置くルール違反として、コンパイルエラーになる
C. `long c = 500_;` は、末尾にアンダースコアがあるため、「不正なアンダースコアです」というコンパイルエラーになる
D. `double d = 6_.02;` は、小数点の直前にアンダースコアがあるため、コンパイルエラーになる
E. `double e = 6.0_2;` は、小数点の直後にアンダースコアがあるため、コンパイルエラーになる
F. `long f = 200_0L;` は、サフィックス`L`の直前にアンダースコアがあるため、コンパイルエラーになる
G. `int g = 0x_4D;` は、16進数プレフィックス`0x`の直後にアンダースコアを置いているため、コンパイルエラーになる
H. `int h = 0_x7B;` は、`0x`というプレフィックスの途中にアンダースコアを割り込ませた書き方であり、これも「不正なアンダースコアです」というコンパイルエラーになる
I. `int i = 0_46;` は、先頭の`0`が`0x`や`0b`と同じ「特別なプレフィックス」として扱われるため、直後にアンダースコアを置くとコンパイルエラーになる
J. `double j = 9e_12;` は、指数を表す`e`の直後にアンダースコアを置いているため、コンパイルエラーになる

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：B, E, F, I
正解：B, E, F, I
迷ったポイント：なし。前回の問題2で見落としたB（先頭アンダースコア＝識別子誤認）のパターンを今回は正しく拾えた。

---

<a id="q3"></a>
## 問題3

**要点**

- アンダースコアは値そのものには一切影響しない。数字と数字の間にあるアンダースコアは、コンパイル時に単純に無視されるだけ
- `println`などで出力されるとき、`int`/`double`はどの進数（2進数・8進数・16進数）で書かれたリテラルであっても、**常に10進数の値に変換されてから**出力される（書いた見た目の桁がそのまま表示されるわけではない）
- 8進数の先頭`0`は特別なプレフィックスではないので、アンダースコアの合否判定にも出力結果にも影響しない（`0_172`は8進数の172＝10進数の122）
- **位取り計算の具体例**：アンダースコアを除去した上で、右端の桁を`(その進数)⁰`、その左を`(その進数)¹`……として、各桁の数字にその重みを掛けて全部足す

  ```
  a = 0b1_010_011（2進数）→ 83
  桁:  2⁶ 2⁵ 2⁴ 2³ 2² 2¹ 2⁰
  値:   1  0  1  0  0  1  1
      = 64 + 0 + 16 + 0 + 0 + 2 + 1 = 83

  b = 0_172（8進数）→ 122　※先頭の0は「8進数」の目印であり、値の計算には使わない
  桁:  8²  8¹  8⁰
  値:   1   7   2
      = 1×64 + 7×8 + 2×1 = 64 + 56 + 2 = 122

  c = 0x2_C（16進数）→ 44　※A〜Fは10〜15に対応（C = 12）
  桁: 16¹ 16⁰
  値:  2   C(=12)
     = 2×16 + 12×1 = 32 + 12 = 44
  ```

次のコードの実行結果として正しいものを1つ選んでください。

```java
public class Main {
    public static void main(String[] args) {
        int a = 0b1_010_011;
        int b = 0_172;
        int c = 0x2_C;
        double d = 4_2.0_5;
        System.out.println(a + ", " + b + ", " + c + ", " + d);
    }
}
```

A. `83, 122, 44, 42.05`
B. `1010011, 172, 2C, 42.05`
C. `83, 172, 44, 42.05`
D. コンパイルエラーが発生する

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A
正解：A
迷ったポイント：なし（正解後、2進数・8進数の位取り計算のやり方について確認質問あり——参考「進数変換の計算方法」に追記）

---

<a id="q4"></a>
## 問題4

**要点**

- 問題3までの「進数変換」「アンダースコアは無視」に加え、もう1つの罠：`+`演算子は、**一度でも文字列(`String`)が左側に現れると、それ以降の`+`はすべて「文字列連結」になる**（数値同士の足し算には戻らない）
- 評価は**左から右へ順番に**行われる。`"文字" + 数値 + 数値`の場合、`"文字" + 数値`の時点で結果が文字列になるため、その後ろの`+ 数値`は連結（文字の末尾にそのまま数字が繋がる）になり、足し算にはならない
- 数値だけが先に並ぶ`数値 + 数値 + "文字"`のような場合は、逆に先に数値同士の足し算が行われてから文字列と連結される（今回のコードとは並び順が違う点に注意）

次のコードの実行結果として正しいものを1つ選んでください。

```java
public class Main {
    public static void main(String[] args) {
        int a = 0b1_0_11;
        int b = 0_37;
        int c = 0x1_A;
        System.out.println("合計:" + a + b + c);
    }
}
```

A. `合計:113126`
B. `合計:68`
C. `合計:113726`
D. コンパイルエラーが発生する

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A
正解：A
迷ったポイント：なし

---

<a id="q5"></a>
## 問題5

**要点**

- 指数表記`数値e指数`は「`数値 × 10^指数`」を意味する
- 指数が**プラス**→小数点を**右**にずらす（値が大きくなる）／指数が**マイナス**→小数点を**左**にずらす（値が小さくなる）
- ずらす桁数は指数の絶対値と同じ（例：`e-4`なら4桁分左にずらす、`e3`なら3桁分右にずらす）

次のコードの実行結果として正しいものを1つ選んでください。

```java
public class Main {
    public static void main(String[] args) {
        double a = 31415e-4;
        double b = 2.5e3;
        double c = 7e-2;
        double d = 1.2e5;
        System.out.println(a + ", " + b + ", " + c + ", " + d);
    }
}
```

A. `3.1415, 2500.0, 0.07, 120000.0`
B. `314150000.0, 0.0025, 700.0, 0.000012`
C. `31.415, 250.0, 0.7, 12000.0`
D. コンパイルエラーが発生する

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：A
正解：A
迷ったポイント：なし。正解後、「doubleが10⁷を超えると指数表記に切り替わる」という別の検証済みルールも絡めたエッジケースを盛り込んでほしいという要望あり（次回以降の指数系問題で反映予定）

---

<a id="q6"></a>
## 問題6

**要点**

- 進数の見分け方：`0x`/`0X`→16進数、`0b`/`0B`→2進数、`0`のみ＋数字が続く→8進数、`0`単体または`1〜9`始まり→10進数
- 8進数はハマりやすい：先頭が`0`だけ（`0x`/`0b`でない）の場合は8進数として解釈される（見た目の桁と値が一致しない）
- 指数表記（`e`/`E`）：例えば`271e-3`は「`271×10⁻³`」の意味。指数がマイナスなら値は小さくなる（割り算）。`-`は装飾ではなく本物のマイナス符号

以下の説明のうち、正しいものはどれですか。（3つ選択）

```java
public class Main {
    public static void main(String[] args) {
        int a = 0x2A;
        int b = 0b1011;
        int c = 0256;
        int d = 256;
        double e1 = 271e-3;
    }
}
```

A. `a`は16進数リテラルであり、10進数で52を表す
B. `b`は2進数リテラルであり、10進数で11を表す
C. `c`は先頭が0であるため8進数として解釈され、10進数で256を表す
D. `d`は先頭が1〜9で始まるため10進数として解釈され、10進数で256を表す
E. `e1`は`271×10³`を意味し、10進数の値は`271000`である
F. `e1`は`271×10⁻³`を意味し、10進数の値は`0.271`である

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：B, D, F
正解：B, D, F
迷ったポイント：なし（正解後、aの値を確認する質問あり——42であることを回答済み）

---

<a id="q7"></a>
## 問題7

**要点（3桁以上の多桁変換、要注意）**

- 桁が増えても考え方は同じ：**右端の桁が常にその進数の0乗（＝1）**、そこから左に1桁進むごとに指数が1つずつ増えていく
- 3桁の16進数`XYZ`なら、`X×16² + Y×16¹ + Z×16⁰`。**Xの指数は「桁数-1」**（3桁なら2乗、4桁なら3乗）になる。「全部の桁に同じ16¹を掛ける」という計算は誤り
- 8進数・2進数でも同じ：それぞれ`8²/8¹/8⁰`、`2⁷/2⁶/…/2⁰`のように、桁ごとに指数が1つずつ増える

次のコードの実行結果として正しいものを1つ選んでください。

```java
public class Main {
    public static void main(String[] args) {
        int a = 0x1_B3;
        int b = 0b1101_0010;
        int c = 0_572;
        System.out.println(a + ", " + b + ", " + c + ", " + (a + c));
    }
}
```

A. `435, 210, 378, 813`
B. `195, 210, 98, 293`
C. `435, 210, 378, 803`
D. コンパイルエラーが発生する

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：D
正解：A
迷ったポイント：`b`の2進数変換（2⁷+2⁶+2⁴+2¹=210）は自力で正しく計算できていたが、最終的にDと誤答。個々の値の計算が合っていても「コンパイルエラーだろう」と反射的に結論づけてしまう傾向があり、4つのNGパターン（先頭・末尾／小数点前後／サフィックス直前／0x・0bプレフィックスの途中・直後）のどれにも該当しないことを確認する一手間を忘れがち。

---

<a id="q8"></a>
## 問題8

**要点**

- テキストブロックは`"""`で開始・終了する複数行文字列リテラル（Java 15で正式導入）
- 閉じデリミタ判定ルール：エスケープされていない`"`が3つ連続した瞬間、その時点で閉じデリミタとみなされる
- `"`が1個・2個連続はそのままOK。3個以上連続させたい場合は少なくとも1つエスケープが必要

次の6つの独立した宣言のうち、コンパイルエラーになるものはどれですか。（2つ選択）

```java
String a = """
        He said "hello" today
        """;

String b = """
        She said ""great"" indeed
        """;

String c = """
        Say """boom""" now
        """;

String d = """
        Value: \"""middle\"""
        """;

String e = """
        End with four quotes: """"
        """;

String f = """
        \"\"\"triple\"\"\"
        """;
```

A. a　B. b　C. c　D. d　E. e　F. f

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：C, E
正解：C, E
迷ったポイント：なし

---

<a id="q9"></a>
## 問題9

**要点**

- 閉じデリミタ判定は「最初に現れた3連続クォート」の時点で即座に発動する。それ以降に何が続いても、その時点で文字列はすでに終わっている
- 「`"`の合計個数」ではなく「連続しているかどうか」がすべて。離れていれば何個あっても無関係

次の説明のうち、**誤っているもの**はどれですか。（2つ選択、問題8と同じa〜fが対象）

```java
String a = """
        He said "hello" today
        """;

String b = """
        She said ""great"" indeed
        """;

String c = """
        Say """boom""" now
        """;

String d = """
        Value: \"""middle\"""
        """;

String e = """
        End with four quotes: """"
        """;

String f = """
        \"\"\"triple\"\"\"
        """;
```

A. `d`は前後とも「エスケープ1つ＋生2つ」の組み合わせで3連続の生クォートが存在しないためコンパイルが通り、中身は`"""middle"""`になる
B. `a`は`"`が1文字ずつ離れて2箇所あるだけで3連続にはならないため、コンパイルは通る
C. `f`はクォート6個全てをエスケープしているため冗長ではあるが問題なくコンパイルでき、中身は`"""triple"""`になる
D. `b`は開始と終了のクォートを合わせて4連続の生クォートとみなされてしまうため、コンパイルエラーになる
E. `e`は最初の3つが閉じデリミタとして解釈され、残った1つのクォートが後続コードの一部として扱われるためコンパイルエラーになる
F. `c`は3連続クォートが前後2箇所にあるが、後半の3連続は単なる引用符として無視されるためコンパイルエラーにはならない

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：B, C, E
正解：D, F
迷ったポイント：B・C・Eは全て正しい説明文なのに誤りとして選んでしまい、逆に本当に誤っているD・Fを見逃した。閉じデリミタが「最初の3連続で即座に確定する」という点、および「連続しているか」だけを見る（合計個数は無関係）という理解が甘かった。→ [pitfalls/2_Javaの基本データ型と文字列の操作.md](../pitfalls/2_Javaの基本データ型と文字列の操作.md)に追加。

---

<a id="q10"></a>
## 問題10

**要点**

- 各行末尾の空白は自動でトリムされる（`\s`未使用の場合）
- 閉じ`"""`が独立行にあるか内容と同じ行にあるかで末尾の改行の有無が変わる（独立行→改行あり、同じ行→改行なし）
- 共通の左端インデントは全行から一律に取り除かれる（閉じデリミタの位置も基準に含めて最小インデント量が決まる）

次のコードの実行結果として正しいものを1つ選んでください。

```java
public class Main {
    public static void main(String[] args) {
        String g = """
                Line1   
                Line2
                """;
        String h = """
                OnlyLine""";
        String i = """
                    Alpha
                Beta
                """;
        System.out.println(g);
        System.out.println(h);
        System.out.println(i);
    }
}
```

A.
```
Line1
Line2
OnlyLine
Alpha
Beta
```

B.
```
Line1
Line2

OnlyLine

    Alpha
Beta
```

C.
```
Line1
Line2

OnlyLine
    Alpha
Beta
```

D.
```
Line1   
Line2

OnlyLine
    Alpha
Beta
```

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：C
正解：C
迷ったポイント：なし

---

<a id="q11"></a>
## 問題11

**要点**

- `\s`：行末の空白を明示的に保持するエスケープ（通常は自動トリムされる）
- 行末の`\`（バックスラッシュのみ）：**その行と次の行**の間の改行を抑制する（行の結合）
- 閉じ`"""`の字下げが浅いほど、共通インデントの基準が小さくなり、内容側の余分な空白がより多く残る
- 中身が空のテキストブロックは`null`ではなく空文字列`""`になる

次のコードについて、説明のうち**誤っているもの**はどれですか。（2つ選択）

```java
public class Main {
    public static void main(String[] args) {
        String t4 = """
                Red    \s
                Black \
                \"""White\"""";
        String t5 = """
                    "11"
                        "17"
                    """;
        String t6 = """
                    "11"
                        "17"
              """;
        String t7 = """
                """;
        System.out.println(t4);
        System.out.println(t5);
        System.out.println(t6);
        System.out.println(t7 == null);
    }
}
```

A. `t4`の出力は2行になり、1行目は`Red`の後ろに合計5個の空白が続く（4個はソース上の直接の空白、1個は`\s`によるもの）
B. `t4`の2行目末尾の`\`（行末バックスラッシュ）は1行目と2行目の間の改行を抑制するため、実際には`Red`と`Black`は1行にまとまって出力される
C. `t5`は`"11"`と`"17"`の間に4文字分のインデント差が保持され、`"17"`の方が余分に右へずれて出力される
D. `t6`は`t5`より閉じ`"""`が浅いインデントで書かれているため、共通インデントの基準がより小さくなり、結果として`t6`の方が`t5`より各行に多くの空白が残って出力される
E. `t7`は中身が空のテキストブロックであり、`t7 == null`は`true`になる
F. `t4`の中身には最終的に生のダブルクォートが4個含まれる（`"""White`の3個＋`White`直後の1個）

<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>

**実施記録**

回答：B, E
正解：B, E
迷ったポイント：なし

---

<a id="ref-conversion"></a>
## 参考：進数変換の計算方法

問題3の`a`（2進数）・`b`（8進数）に関連して、実際に計算方法を確認した内容。`c`（16進数）も同じ考え方。

**共通の手順**：アンダースコアを除去した上で、右端の桁を`(その進数)⁰`、その左を`(その進数)¹`……として、各桁の数字にその重みを掛けて全部足す。

**a = `0b1_010_011`（2進数）→ 83**

```
桁:  2⁶ 2⁵ 2⁴ 2³ 2² 2¹ 2⁰
値:   1  0  1  0  0  1  1
    = 64 + 0 + 16 + 0 + 0 + 2 + 1 = 83
```

**b = `0_172`（8進数）→ 122**

先頭の`0`は「これは8進数」という目印（プレフィックス）であり、値の計算には使わない。アンダースコアを除去した残りの`172`だけを8進数として計算する。

```
桁:  8²  8¹  8⁰
値:   1   7   2
    = 1×64 + 7×8 + 2×1 = 64 + 56 + 2 = 122
```

**c = `0x2_C`（16進数）→ 44**

16進数の`A〜F`は`10〜15`に対応する（`C = 12`）。

```
桁: 16¹ 16⁰
値:  2   C(=12)
   = 2×16 + 12×1 = 32 + 12 = 44
```
