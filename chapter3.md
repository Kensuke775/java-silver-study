# 第3章 演算子と条件分岐

対応する教科書ページ: （要確認・進行中）
学習環境: macOS（Apple Silicon）／ JDK 17（Homebrewの`openjdk@17`、17.0.20）

---

## 1. シフト演算子（`<<`, `>>`, `>>>`）

2進数の**ビット列そのものを左右に物理的にずらす**演算子。


| 演算子   | 意味                               |
| ----- | -------------------------------- |
| `<<`  | 左シフト（ビットを左にずらす＝**×2**を繰り返すのと同じ）  |
| `>>`  | 符号付き右シフト（ビットを右にずらす＝**÷2**、符号を維持） |
| `>>>` | 符号なし右シフト（ビットを右にずらす、符号を無視して常に0埋め） |


### 1.1 正の数での挙動（仕組みの基本）

`8`は2進数で`1000`。左右にずらすと以下のようになる（実際に検証済み）。

```
8  = 00001000

8 << 1 = 00010000 = 16   ← 全ビットが1つ左にずれる（右端に0を1個補充）
8 << 2 = 00100000 = 32   ← 2つ左にずれる（右端に0を2個補充）
8 << 3 = 01000000 = 64   ← 3つ左にずれる（右端に0を3個補充）

8 >> 1 = 00000100 = 4    ← 全ビットが1つ右にずれる
8 >> 2 = 00000010 = 2    ← 2つ右にずれる
8 >> 3 = 00000001 = 1    ← 3つ右にずれる
```

**なぜ「×2」「÷2」になるのか**：2進数は「1桁左にずれる＝その桁の重みが2倍になる」という仕組み（10進数で`8`を1桁左にずらして`80`にすると10倍になるのと同じ理屈、基数が2であるだけ）。

一般化すると、**Nビットずらす＝2のN乗倍／2のN乗分の1**という関係になる。

```
8 << 2 = 8 × 2² = 8 × 4 = 32   ← 一致
8 >> 2 = 8 ÷ 2² = 8 ÷ 4 = 2    ← 一致
```

`a << n`は「`a`を`2ⁿ`倍する」、`a >> n`は「`a`を`2ⁿ`で割って小数点以下切り捨てる」計算の、ビット演算による実装。掛け算・割り算より高速に処理できるため、ハッシュ計算やビットフラグ操作など性能が重要な場面で使われる。

### 1.2 負の数での挙動（`>>`と`>>>`の本当の違い）

正の数だと`>>`と`>>>`は同じ結果になるが、**負の数で挙動が分かれる**。ここが試験の最頻出ポイント。

`-8`を32bitの2進数で表すと`11111111111111111111111111111000`（負の数は2の補数表現なので、先頭ビットが1で埋まる）。

```
-8 << 1  = -16           (2進数: 11111111111111111111111111110000)  ← 単純に×2、符号も維持

-8 >> 1  = -4            (2進数: 11111111111111111111111111111100)  ← 符号(負)を維持
-8 >> 2  = -2            (2進数: 11111111111111111111111111111110)  ← 符号を維持
-8 >> 3  = -1            (2進数: 11111111111111111111111111111111)  ← 符号を維持

-8 >>> 1 = 2147483644    (2進数:  1111111111111111111111111111100)  ← 符号を無視、0埋め
-8 >>> 2 = 1073741822    (2進数:   111111111111111111111111111110)  ← 符号を無視、0埋め
-8 >>> 3 = 536870911     (2進数:    11111111111111111111111111111)  ← 符号を無視、0埋め
```

- `<<`：正負に関係なく単純に`×2ⁿ`。左シフトは桁が増える方向の操作なので情報が失われず、符号もそのまま維持される（`>>`のような切り捨て方向の特別ルールは不要）
- `>>`：空いた左側のビットを**符号ビット**（正なら0、負なら1）で埋める → 符号が保たれる
- `>>>`：空いた左側のビットを**常に0**で埋める → 負の数に使うと突然巨大な正の数に化ける

「負の数に`>>>`を使うと激変する」という点を、実際の数値と一緒に覚えておくこと。

### 1.3 手計算のショートカット（実際の値を求めたい時）

32bitの2進数を毎回書き出す必要はない。

**`>>`（符号付き右シフト）**：「`÷2ⁿ`、ただしマイナス無限大方向に切り捨て」で計算できる。

```
-8 >> 2  = -8 ÷ 4 = -2
```

注意点は、`>>`は「0に近づく方向」ではなく「マイナス無限大に近づく方向」に切り捨てる（floor）という点。通常の`/`演算子（0方向に切り捨て）とは丸め方向が異なる。

```
-7 >> 1  = -4      ← マイナス方向に切り捨て（floor）
-7 / 2   = -3      ← 0方向に切り捨て（実際に検証済み、結果が異なる）
```

8や16のような**2の累乗ちょうどの数**なら誤差なく単純に「÷2ⁿ」でよい。

**`>>>`（符号なし右シフト）**：「先に`2³²`(4294967296)を足してから`2ⁿ`で割る」で計算できる。

```
-8 >>> 2 の場合：
① -8 + 4294967296 = 4294967288   ← 「符号なしとして見た時の値」になる
② 4294967288 ÷ 4 = 1073741822    ← 実際の実行結果と一致（検証済み）
```

ただし試験で正確な巨大数値まで暗算させる問題はまず出ない。「負の数に`>>>`を使うと符号が無視されて巨大な正の数になる」という**性質**さえ分かっていれば、選択肢問題では消去法で対応できる。

```bash
# 実際に確認したコード（scratchpad内、ShiftNeg.java相当）
javac ShiftNeg.java && java ShiftNeg
```

---



## 2. 複合代入演算子（`^=`, `|=`, `<<=`, `>>=`, `>>>=`）

すべて「`a 演算子= b`」は「`a = a 演算子 b`」の省略形、という共通パターン。


| 演算子        | 展開すると         | 意味                   |
| ---------- | ------------- | -------------------- |
| `a &= b`   | `a = a & b`   | ビット単位のAND：両方1なら1     |
| `a ^= b`   | `a = a ^ b`   | ビット単位のXOR：同じなら0、違えば1 |
| `a |= b`   | `a = a | b`   | ビット単位のOR：どちらかが1なら1   |
| `a <<= b`  | `a = a << b`  | 左シフト代入               |
| `a >>= b`  | `a = a >> b`  | 符号付き右シフト代入           |
| `a >>>= b` | `a = a >>> b` | 符号なし右シフト代入           |


```
1010 ^= 0110 → 1100   （同じ桁は0、違う桁は1）
1010 |= 0110 → 1110   （どちらかが1なら1）
```



### 2.1 複合代入演算子には「隠れたキャスト」がある（重要）

```java
byte c = 10;
c += 5;   // OK。実際は c = (byte)(c + 5); と同じ扱いになる（実際に検証済み、結果は15）
```

本来`c + 5`は`int`同士の演算になるため、これを`byte`型の`c`に代入しようとすると通常は縮小変換でコンパイルエラーになるはず。しかし複合代入演算子（`+=`, `^=`など）は**自動的に暗黙のキャストを挿入してくれる**という特別な性質を持つ。

対比として、複合代入演算子を使わずに同じことを書くと本当にコンパイルエラーになる：

```java
byte c = 10;
c = c + 5;   // コンパイルエラー（int→byteの縮小変換、明示的なキャストが必要）
c = (byte)(c + 5);  // これなら通る
c += 5;              // 複合代入演算子ならこれと同じ意味になり、暗黙にキャストされるためOK
```

---



## 3. キャストと`instanceof`



### 3.1 キャスト（明示的な型変換）

```java
double d = 3.14;
int i = (int) d;   // (型名) を値の直前に置く。小数点以下切り捨てで i = 3
```

`(型名)`を変換したい値の直前に置く構文。**縮小変換**（`double→int`、`long→int`など、情報が失われる可能性がある変換）をする際、コンパイラに「わかった上でやっている」ことを明示するために必須になる。

### 3.2 `instanceof`（型の実行時判定）

```java
Object obj = "Hello";
if (obj instanceof String) {
    System.out.println("Stringです");
}
```

その変数が実際にはどのクラスのインスタンスかを**実行時**に判定し、`boolean`を返す演算子。主に、参照型変数を**ダウンキャスト**（`Object`型を`String`型に戻す等）する前に、安全かどうかを確認する目的で使われる。

Java 16以降は、判定と同時に変数へ束縛できるパターンマッチング構文も追加されている：

```java
if (obj instanceof String s) {
    System.out.println(s.length());  // instanceofがtrueなら、sという名前でString型として使える
}
```

---



## 4. char型の型変換（教科書107ページ、表3-8）

### 4.1 数値 → char型

`boolean`型を除く基本データ型はすべて`(char)`キャストで`char`型に変換できる。変換される数値は**Unicodeの文字コード**として解釈される（`char`の範囲は`\u0000`〜`\uffff`＝10進数で`0`〜`65535`）。

```java
byte b = 97;             (char)b   → a
short s = 98;            (char)s   → b
int i = 99;              (char)i   → c
long l = 100L;           (char)l   → d
float f = 101.123F;      (char)f   → e
double d = 102.456;      (char)d   → f
```

（実際に検証済み、上記すべて一致）

元の型が`byte`でも`double`でも関係なく、**必ず`(char)`の明示的キャストが必要**（`int→char`のような「一見ワイドニングに見えるケース」でも自動変換されない点に注意。`char`は符号なし16bit、`int`は符号付き32bitで表現の仕組みが違うため）。`float`/`double`の場合は小数点以下切り捨て後の整数部分が文字コードとして解釈される（例：`101.123`→`101`→`'e'`）。

### 4.2 char型 → 数値型（逆方向）

```java
char g = 'g'; byte  charb = (byte)g;   // 103   ← キャスト必須
char h = 'h'; short chars = (short)h;  // 104   ← キャスト必須
char i = 'i'; int   chari = i;         // 105   ← キャスト不要（暗黙の型変換）
```

（実際に検証済み、上記すべて一致）

- `char → byte`／`char → short`：**キャストが必要**。`char`は`0〜65535`（符号なし16bit）だが、`byte`（`-128〜127`）・`short`（`-32768〜32767`）は`char`より表現範囲が狭いため、ナローイング扱いになる。
- `char → int`：**キャスト不要**。`int`（符号付き32bit）は`char`の全範囲を余裕で表現できるため、ワイドニングとして暗黙変換される。

### 4.3 参考：試験対策の暗記ポイント（教科書107ページコラム）

アルファベット小文字の`'d'`が文字コード`100`であることを覚えておくと、そこからの増減で他の小文字アルファベットの文字コードを計算できる（例：`'a'`=97, `'g'`=103, `'z'`=122）。

---



## 5. 基本データ型から参照型への型変換（教科書109〜110ページ）

### 5.1 ラッパークラス（表3-9）

基本データ型を「オブジェクト（参照型）」として扱うための専用クラス。`java.lang`パッケージ所属なので`import`不要。

| 基本データ型 | ラッパークラス |
|---|---|
| byte | Byte |
| short | Short |
| int | **Integer**（`Int`ではない点に注意） |
| long | Long |
| float | Float |
| double | Double |
| char | Character |
| boolean | Boolean |

### 5.2 オートボクシング／オートアンボクシング

基本データ型 ⇔ 対応するラッパークラスの変換は**自動的に**行われる。

```java
Integer obj1 = 100;    // オートボクシング：int → Integer（自動）
int i1 = obj1;         // オートアンボクシング：Integer → int（自動）
```

（実際に検証済み、`obj1 : i1` = `100 : 100`）

**重要**：これは基本データ型同士のキャスト（ワイドニング／ナローイング）とは別の仕組み。自動変換が起きるのは**対応する型のペアのみ**で、「数値系だからなんとなく変換できるだろう」は通用しない。

```java
Double obj2 = 10;   // コンパイルエラー
```

```
エラー: 不適合な型: intをDoubleに変換できません
```

（実際に検証済み、本当にコンパイルエラーになった）`int→double`は基本データ型同士のワイドニングとしてOKだが、`int→Double`（ラッパークラス）はオートボクシングの対象外（オートボクシングは`int→Integer`限定）。

### 5.3 文字列 ⇔ 基本データ型の変換

```java
int i = Integer.parseInt("123");           // 文字列 → int
double d = Double.parseDouble("1.23");     // 文字列 → double
boolean b = Boolean.parseBoolean("true");  // 文字列 → boolean
char c = "Duke".charAt(0);                 // 文字列の1文字目 → char（D）
```

- `ラッパークラス名.parseXxx(文字列)`：`char`型以外の全型に対応（`parseInt`, `parseDouble`, `parseBoolean`など）
- `char`型だけ`parseChar`は存在しない → `文字列.charAt(インデックス)`を使う
- `parseBoolean`は大文字小文字を区別しない（`"TRUE"`や`"True"`も`true`扱い）。`"true"`（大文字小文字問わず）以外の文字列はすべて`false`になる

---



## 疑問点・要復習（随時追記）

- 

