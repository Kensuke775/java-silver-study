# 第5章 クラスとメソッドの基礎

対応する教科書ページ: （要確認・進行中）
学習環境: macOS（Apple Silicon）／ JDK 17（Homebrewの`openjdk@17`、17.0.20）
サンプル: `~/Downloads/sample/chap5/`

---

## 1. プリミティブ型 vs 参照型と`null`の許容

Javaの変数は2種類に分かれ、`null`を入れられるかどうかはこの区別だけで決まる（型の種類は無関係）。

- **プリミティブ型**（int, double, boolean, charなど）：箱の中に**値そのもの**が直接入っている。`null`（＝何も指さない状態）を表現する方法がなく、`int[] numbers = {1, null, 3};`のように書くと**コンパイルエラー**（`incompatible types: <null> cannot be converted to int`）。
- **参照型**（String, 配列, クラスのインスタンス全般）：箱の中には値の実体ではなく「実体が置かれている場所（住所）」が入っている。「どこも指していない」＝`null`を表現できる。

```java
int[] numbers = {1, null, 3};    // NG：値そのものを入れる箱にnullは入らない
Integer[] nums = {1, null, 3};   // OK：Integerは参照型（intのラッパークラス）だから住所を入れる箱
```

`Integer`は`int`の値を包んだクラス（オブジェクト）＝参照型なので、`null`を許容できる。「Stringだから」ではなく「参照型だから」が正しい理由づけ。

### 1.1 コンパイルエラーと実行時エラーの違い（重要）

```java
int x = null;              // コンパイルエラー（ビルド時に検出、実行すらできない）

String city = null;
city.equals(null);          // コンパイルは通る。実行すると NullPointerException（実行時エラー）
city == null;                // 常に安全。エラーにならない
```

| コード | エラーの種類 | 発生タイミング |
|---|---|---|
| `int x = null;` | コンパイルエラー | ビルド時 |
| `city.equals(null)`（cityがnull） | 実行時例外（NPE） | 実行してその行に到達した瞬間 |
| `city == null` | エラーなし | 常に安全 |

`==`は参照型同士でも「住所が同じか」を比較するだけの演算子であり、メソッド呼び出しではないためnullでも安全。`.equals()`はメソッド呼び出しなので、呼び出し元がnullだと即NPE。

---

## 2. Integerのキャッシュ範囲（-128〜127）と`==`／`.equals()`

```java
Integer x = 1000;
Integer y = 1000;
x == y;          // false（別オブジェクトの住所を比較している）
x.equals(y);     // true（値を比較している）
```

`Integer`同士の`==`は住所比較。中身が同じ値でも、別々に作られたオブジェクトなら`false`になる。**値を比較したいときは常に`.equals()`を使う**。

### 2.1 -128〜127だけは例外（IntegerCache）

`Integer`クラスは内部に`IntegerCache`を持ち、オートボクシング時（`Integer x = 1;`や`Integer.valueOf(1)`）、**-128〜127の範囲の値だけ、新しいオブジェクトを作らずキャッシュ済みの同じオブジェクトを使い回す**。

実際に検証済み（JDK 17）：

```
126 -> true   (同じオブジェクト)
127 -> true   (同じオブジェクト)
128 -> false  (別オブジェクト)
129 -> false  (別オブジェクト)
130 -> false  (別オブジェクト)
```

JVMの内部フラグ`AutoBoxCacheMax = 128`（`-XX:+PrintFlagsFinal`で確認可能）とも一致。

**重要な注意点**：これは「型が参照型として特殊になる」わけではなく、**Integerクラスというライブラリ実装の最適化**にすぎない。null許容のルール（参照型かどうかだけで決まる）とは完全に無関係の、別次元の話。「-128〜127だけnullの例外」という繋げ方はしない。値が何であれ`Integer`型ならnullは常に許容される。

**実務・試験どちらでも安全な運用**：`Integer`同士の値比較は範囲を暗記するより、常に`.equals()`を使う。

---

## 3. 可変長引数（varargs）の実体は配列

```java
public void method(int i, int... j) {
    for (int v : j) { System.out.print(v); }
}
```

`型... 変数名`は**コンパイラが内部的に`型[] 変数名`として扱うための糖衣構文（シンタックスシュガー）**。実際に検証済み：

```java
method(1, 2, 3, 4);   // j.getClass() = [I（int配列）, j.length = 3
method(1, arr);        // 配列を直接渡してもOK。同じく [I, length = 3
method(1);              // j.getClass() = [I, j.length = 0（nullではなく空配列）
```

- `method(1, 2, 3, 4)`のように個別の引数で呼ぶと、コンパイラが自動で`new int[]{2,3,4}`を組み立てて渡す。
- 呼び出し側は配列を作らなくてよい利便性を得られるが、メソッド内部では**最初から最後まで普通の配列**として扱われる（`.length`, 拡張for文, 添字アクセスすべて通常通り使える）。
- varargs部分は0個でも合法（空配列になるだけで、コンパイルエラーにはならない）。**必須なのはvarargsより前にある固定引数の分だけ**（上記なら`i`の1個のみ）。

---

## 4. メソッドのオーバーロード解決の優先順位

```java
static void method(int i, int... j) { ... }
static void method(int i, int j) { ... }

method(100, 200);   // 固定2引数版が呼ばれる
method(1, 2, 3);     // varargs版が呼ばれる
method(100);          // varargs版が呼ばれる（j = 空配列）
```

「渡した引数の個数で自動的に振り分けられる」わけではなく、**Javaのオーバーロード解決には優先順位（フェーズ）がある**。

1. **フェーズ1**：ボックス化もvarargsも使わず、そのまま型が完全一致するメソッドがあるか
2. **フェーズ2**：オートボクシング／アンボクシングを使えば一致するメソッドがあるか
3. **フェーズ3**：それでも見つからなければvarargsメソッドを候補にする（最後の手段）

`method(100, 200)`は`method(int, int)`がフェーズ1で完全一致するため即決定。varargs版はフェーズ3まで検討される前に候補から外れる。`method(1, 2, 3)`は個数が合う固定引数メソッドが存在しないため、フェーズ1・2で候補がゼロになり、フェーズ3でvarargs版が採用される。

---

## 5. パッケージ宣言とディレクトリ構造

```java
package com;

public class Item { ... }
```

- `package com;`と書くと、そのクラスの正式名称は`Item`ではなく**`com.Item`**になる。パッケージは名前空間であり、**同名クラスの衝突を防ぐ**ための仕組み。
- パッケージ名とフォルダ構成は一致させる規約がある（`package com;`なら`com`という名前のフォルダの中にファイルを置く）。
- 別パッケージのクラスを使うには`import`が必要（例：`import com.Item;`）。書かないと「シンボルが見つからない」でコンパイルエラー。
- パッケージ構造があるプロジェクトのコンパイル・実行は、単純な`javac Main.java`では済まず、出力先（`-d`）やクラスパス（`-cp`）を意識する必要がある。

```bash
cd sources
javac -d ../classes Main.java com/Item.java
java -cp ../classes Main
```

---

## 6. IDEのエラー表示とjavacの実際のコンパイルエラーの違い

サンプル教材（`~/Downloads/sample/`）は`package`宣言なしのファイルが章ごとに大量にあり、同名クラス（`Sample`が8箇所、`Main`も多数）が無名パッケージに散らばっている。

- VS CodeのJava拡張機能（Language Server、Eclipse JDTベース）が、ワークスペース全体を1つの無名パッケージとして解析しようとすると、**同名クラスの衝突により誤った型解決・誤ったエラー表示**が起きることがある（実際に`setNum(int)`が「undefined」と表示されたが、`javac`で直接コンパイル・実行すると問題なく動作するケースを確認）。
- IDEのエラーメッセージ（`The method X is undefined for the type Y`という言い回し）はEclipse JDT特有のもので、javac本来のエラー（`cannot find symbol`）とは文言が異なる。**IDEの赤い波線＝実際のコンパイルエラーとは限らない**。
- 確実な検証方法は、対象フォルダでターミナルから直接`javac`・`java`を実行すること。

```bash
cd ~/Downloads/sample/chap5/2
javac Main.java Sample.java && java Main
```

**根本的な対策**：この教材のように同名クラスが大量にある場合、各章・各問題ごとにパッケージ宣言を分ける（本章5節）ことで、IDE側の型解決の混乱自体を避けられる。

---

## 7. コンストラクタ

### 7.1 コンストラクタの成立条件

「クラス名と完全に同じ名前」かつ「戻り値の型が一切ない（`void`すら書かない）」の2つを同時に満たすメソッドだけがコンストラクタになる。

```java
public class Item {
    public Item(int id) { ... }        // コンストラクタ（戻り値なし）
    public void Item(int id) { ... }    // ただの同名メソッド（voidと書いた時点でコンストラクタではない）
}
```

`void`を書いた瞬間に「クラス名と同じ名前の普通のメソッド」に変わり、`new Item(...)`では呼ばれず、明示的に`item.Item(...)`のように呼び出さない限り実行されない（実機で検証済み）。

### 7.2 コンストラクタがsetterより優れている理由（単なる利便性ではない）

- カスタムコンストラクタを1つでも定義すると、暗黙のデフォルトコンストラクタ（引数なし）は消える。そのため`new Item()`のような「フィールドが何も設定されない状態のインスタンス生成」自体がコンパイルエラーで防げる。
- setter方式では、setterの呼び忘れがあってもコンパイルは通ってしまい、未設定のフィールドにアクセスした瞬間に実行時エラー（NPEなど）が起きる。コンストラクタ方式は「値が未確定のオブジェクトが外部から見える状態」を構造的に作れない。
- コンストラクタの途中で例外が発生した場合、呼び出し元の変数には**中途半端に構築されたオブジェクトへの参照は一切残らず**、`null`のまま（＝そもそも代入自体が起きない）になることを実機で確認済み。

### 7.3 コンストラクタのオーバーロード（メソッドと同じ一般ルール）

コンストラクタだけに特別に許された機能ではない。「同じ名前で引数の型・個数が異なる版を複数定義できる」というオーバーロードは、通常のメソッドにも等しく使える一般ルールであり、コンストラクタはクラス名以外の名前を付けられないため、複数の初期化パターンを提供する手段として**オーバーロードしか選択肢がない**、という関係になる（「クラス名固定→だからオーバーロードが許される」ではなく、「オーバーロードという一般ルールが先にあり、コンストラクタもそれを使っているだけ」が正しい因果関係）。

```java
public Item() { ... }
public Item(int id, String name) { ... }
public Item(int id, String name, int price) { ... }
```

なお、フィールドやコンストラクタの**宣言順序は動作に一切影響しない**（実機で入れ替えて検証済み）。

### 7.4 `this(...)`によるコンストラクタチェーン

自分自身の別のコンストラクタを呼び出す構文。3つの厳格なルールがある。

**ルール1：`this(...)`はコンストラクタの先頭文でなければならない**

```java
public Item() {
    this.price = 1000;      // NG：this(...)より前に処理がある
    this(0, "T-shirt", 0);
}
```

```
エラー: thisの呼出しはコンストラクタの先頭文である必要があります
```

`this(...)`より**後**に処理を書くのは問題ない。

```java
public Item() {
    this(0, "T-shirt", 0);  // OK：先頭
    this.price = 1000;       // OK：以降は自由（3引数コンストラクタでprice=0が入った後、1000で上書き）
}
```

理由：オブジェクトが完全に構築される前に、そのオブジェクトに対して何らかの処理をさせないため。`this(...)`より前にコードを書けてしまうと、フィールドが未初期化の状態のオブジェクトに対して処理することが可能になってしまう。

**ルール2：呼び出し先は自分自身と異なる引数シグネチャでなければならない**

```java
public Item(int id, String name) {
    this(id, name);   // NG：自分自身と同じシグネチャ = 無限再帰
}
```

```
エラー: 再帰的なコンストラクタ呼出し
```

**ルール3：`this`は予約語であり、メソッド名として使えない**

`this(...)`はメソッド呼び出しではなく、コンストラクタチェーン専用の構文。`this`という識別子を自分でメソッド名に使おうとするとコンパイルエラーになる。

### 7.5 その他

- IDEの「値は使用されていません」（field value not used）警告は、**代入（書き込み）だけでは消えず、どこかで読み取り（参照）されて初めて消える**。`private`フィールドで特に検出されやすいのは、IDEが同一ファイルだけを見て「外部から絶対に参照されない」と確定できるため（`public`だと外部利用の可能性を否定できず警告が出にくい）。

---

## 8. 用語整理：メンバ・フィールド・変数

```java
public class Item {
    private int id;               // フィールド（インスタンス変数）＝メンバ
    private static int count;     // フィールド（クラス変数／static）＝メンバ
    public Item(int id) { ... }   // コンストラクタ（JLS上はメンバに含まない）
    public void printItem() {     // メソッド＝メンバ
        int total = id * 2;        // ローカル変数（フィールドでもメンバでもない）
    }
}
```

- **メンバ（member）**：クラス直下に定義された、フィールドとメソッドの総称（コンストラクタは慣習上メンバに含めないことが多い）。
- **フィールド（field）**：メンバのうち「変数」の部分だけ（インスタンス変数／クラス変数の両方を含む）。
- **ローカル変数**：メソッドの中で宣言した変数。フィールドでもメンバでもない、別カテゴリ。

`変数＝フィールド`ではなく、**クラス直下で宣言された変数だけがフィールド**、という点に注意（メソッド内のローカル変数は対象外）。

---

## 9. static/インスタンスメンバへのアクセス方法

```java
public class Sample {
    int value = 100;                 // インスタンス変数
    static int commonValue = 500;    // static変数
    public void printValue() { ... }         // インスタンスメソッド
    public static void printCommonValue() { ... }  // staticメソッド
}
```

| アクセスの仕方 | staticメンバ | インスタンスメンバ |
|---|---|---|
| `クラス名.メンバ`（例：`Sample.commonValue`） | 〇 正規の書き方 | ✗ コンパイルエラー |
| `参照変数名.メンバ`（例：`s.value`） | △ 動くが非推奨 | 〇 正規の書き方 |

実機で検証済み：

```
エラー: staticでない変数 valueをstaticコンテキストから参照することはできません
エラー: staticでないメソッド printValue()をstaticコンテキストから参照することはできません
```

**なぜ`Sample.value`がNGか**：`クラス名.メンバ`という構文自体が「staticなアクセス」として文法解釈される。`value`はstaticではないインスタンス変数なので、文法的に噛み合わない。`new`で先にインスタンスを作っておいても、この書き方である限りエラーは消えない（実機で確認済み）。

**なぜ`s.commonValue`（△）は動くのに非推奨か**：`s`が指す“中身”は実行時に一切参照されない。コンパイラは`s`の**宣言された型**だけを見て、内部的に`Sample.commonValue`と全く同じコードとして扱う。証拠として、`s`を`null`にしても`NullPointerException`にならず正常に動作することを確認済み。

```java
Sample s = null;
int v = s.commonValue;     // 500が返る。NPEにならない（実機で確認済み）
```

これに対し、`s.value`や`s.printValue()`は実行時に`s`の実体を辿る必要があるため、`s`が`null`なら必ず`NullPointerException`になる。

**逆方向（staticメソッドの中からインスタンスメンバを使おうとする場合）**：

```java
public static void printCommonValue() {
    int val = value;    // NG：staticでない変数 value をstaticコンテキストから参照できない
}
```

`value`のようにクラス名も`this.`も付けずに書くと、Javaは暗黙的に`this.value`として解釈する。しかしstaticメソッドには`this`（今どのインスタンスの処理をしているかという情報）が存在しないため、暗黙のthis補完が失敗してエラーになる。**「staticかどうか」を決めるのはメンバ自身の宣言ではなく、それを使おうとしている側（呼び出し元のメソッド）がstaticかどうか**、という点がポイント。

---

## 10. static初期化子・インスタンス初期化子

```java
class Test {
    static int num;
    String s;
    static {                          // static初期化子
        num = 100;
        System.out.println("static Initializer... num : " + num);
    }
    {                                   // インスタンス初期化子（staticなし）
        this.s = "Test";
        System.out.println("Initializer... s : " + s);
    }
    public Test(String s) {
        this.s = s;
        System.out.println("Constructor... s : " + s);
    }
}
```

`new Test(...)`を3回実行した際の実機での出力（検証済み）：

```
static Initializer... num : 100     ← 1回だけ（1回目のnewの直前）
Initializer... s : Test              ← newのたびに毎回
Constructor... s : 1個目
Initializer... s : Test              ← 2回目
Constructor... s : 2個目
Initializer... s : Test              ← 3回目
Constructor... s : 3個目
```

| | 実行タイミング | 実行回数 |
|---|---|---|
| `static { }` | そのクラスがJVMに初めてロードされた瞬間、自動実行 | クラスにつき1回だけ |
| `{ }`（インスタンス初期化子） | `new`のたびに、コンストラクタ本体の直前 | インスタンス生成のたびに毎回 |
| コンストラクタ本体 | インスタンス初期化子の直後 | インスタンス生成のたびに毎回 |

どちらも明示的に呼び出すコードはどこにも存在せず、JVMが決まったタイミングで裏側で自動実行する。

---

## 11. 参照型変数への代入は「住所のコピー」

```java
Item item1 = new Item("T-shirt");
Item item2 = item1;          // item1の"住所"をコピーするだけ。実体は増えない
item2.name = "Jeans";
System.out.println(item1.name);   // "Jeans"（item1も書き換わる）
System.out.println(item1 == item2);  // true（同じ住所を指しているから）
```

実機で確認済み：`item1, item2 : Jeans, Jeans`、`item1 == item2 : true`。

`item2 = item1`は実体（インスタンス）を複製しているのではなく、`item1`が持つ「住所」という情報をそのまま`item2`にもコピーしているだけ。結果として`item1`と`item2`は別々の変数でありながら、指している実体は1つだけになる。どちらか一方を経由してフィールドを書き換えると、もう一方から見ても変更が反映される。

---

## 12. `protected`の概要（本格的には第6章）

`chap5`ではほぼ登場せず、継承がテーマの`chap6`で本格的に使われる。アクセス修飾子は範囲の狭い順に4段階。

| 修飾子 | アクセスできる範囲 |
|---|---|
| `private` | 同じクラスの中だけ |
| （修飾子なし＝デフォルト） | 同じパッケージの中だけ |
| `protected` | 同じパッケージ ＋ 別パッケージのサブクラス（継承先） |
| `public` | どこからでも |

`private`より緩く`public`より厳しい、継承と関係の深い修飾子として第6章で深掘りする。

---

## 13. 章末問題（ex1）で判明した典型的な間違いパターン

自己採点18問中、つまずいたテーマを整理（正解率3/18）。すべて実機で再現・検証済み。

| # | テーマ | 要点 |
|---|---|---|
| 問7 | `this.`の付け忘れ | `id = id;`（`this.id = id;`ではない）は、引数`id`に引数自身を再代入しているだけ。メンバ変数`id`には何も設定されず、デフォルト値（`0`, `null`）のまま残る（実機で`0:null`を確認済み） |
| 問9 | デフォルトコンストラクタの生成条件 | 「コンストラクタが1つも定義されていないクラス」にのみ自動生成される。`void`付きの同名メソッドはコンストラクタではないので、その場合も自動生成される（実機で確認済み） |
| 問10, 13 | `this(...)`はコンストラクタの先頭行必須 | 先頭行以外に書くと「thisの呼出しはコンストラクタの先頭文である必要があります」でコンパイルエラー |
| 問11 | オーバーロードの判定は型のみ、変数名は無関係 | 引数の型・個数・順序が完全一致していれば、変数名が違うだけでは別メソッドと認められず「重複」でコンパイルエラー |
| 問12, 16 | static/インスタンスメンバのアクセス制限 | `main()`など static メソッドの中から `this` や `クラス名.インスタンスメンバ` は使えない（本章9節参照） |
| 問14 | オーバーロード解決の優先順位 | 完全一致 → 基本データ型へのワイドニング → ラッパークラスへのボクシング → 可変長引数、の順で候補が絞られる（実機で、固定引数優先・float優先(Integerより)・可変長は最後の手段、を確認済み） |
| 問15 | `main()`のオーバーロード | `main()`は他のメソッドと同様オーバーロード可能だが、JVMが起動時に呼ぶのは必ず`public static void main(String[] args)`という決まった1つの形だけ（実機で`11111`を確認済み） |
| 問17 | 拡張for文の変数は値（参照）のコピー | ループ変数への代入（`f = null`など）は、元の配列の中身には一切影響しない（本章とchapter4.mdの拡張for文の内容と同じ原則。実機で確認済み） |

---

## 14. `this(...)`は1回・先頭行のみ（7.4の補足）

```java
public Sample(int x) {
    this(x, 2);   // OK：先頭文
    this();       // NG：2つ目のthis()呼び出し
    System.out.print(x);
}
```

```
エラー: thisの呼出しはコンストラクタの先頭文である必要があります
```

ルールが禁止しているのは正確には「`this()`が2回ある」ことではなく「**`this()`が先頭文でない**」こと。1行目の`this(x, 2)`は先頭文なのでOKだが、2行目に書いた瞬間`this();`はもう先頭文ではなくなるためNG。結果として、コンストラクタ内に書ける`this(...)`は実質1回・必ず1行目だけになる（`super()`も同じ扱い）。

`this(...)`は普通のメソッド呼び出しと同様、渡した引数の**個数と型が完全一致する**コンストラクタを呼ぶ。「n個の引数に合うものが呼ばれる」ではなく「型まで一致するものが呼ばれる」が正確（個数だけ合っていて型が違うオーバーロードは候補外）。またキャスト（`this((long) x, 2)`など）で明示的に呼び分け先を変えることも可能（実機で通常のオーバーロード呼び出しで検証済み）。

---

## 15. オーバーロード解決の補足：名前の一致が最優先、あいまいさはエラー

4節の「フェーズ1（完全一致・ワイドニングのみ）→フェーズ2（ボクシング）→フェーズ3（可変長引数）」の話には、その手前に暗黙の前提がある。

```java
public int foo(int... x) { ... }
public void bar(String... s) { ... }

t.bar();   // fooは絶対に候補にならない（名前が違うから）
```

**同じ名前のメソッドの中でしか**フェーズ1〜3による絞り込みは行われない。名前が違うメソッド同士は、引数の型が何であれ最初から比較対象外。

### あいまい（ambiguous）なら「上から順」ではなくコンパイルエラー

```java
public void bar(String... s) { ... }
public void bar(int... x) { ... }

a.bar();   // 引数0個。どちらのvarargsにも展開できる
```

```
エラー: barの参照はあいまいです
  bar(String...)とbar(int...)の両方が一致します
```

同じフェーズで複数の候補が同時に成立する場合、**ソースコード上の記述順（上から順）で決まるわけではなく、コンパイルエラーになる**（実機で確認済み）。

### 型付き配列を明示的に渡すと、あいまいさは最初から発生しない

```java
t.foo(new int[] {});      // int[]型を明示 → foo(int...)にしかマッチしない
t.foo(new String[] {});   // String[]型を明示 → foo(String...)にしかマッチしない
```

可変長引数`int... x`は内部的に`int[] x`という配列パラメータなので、`int[]`のオブジェクトを直接渡せばそのまま代入されるだけ（型がぴったり一致）。「中身が空かどうか」は無関係で、**配列の型を明示的に確定させて渡しているかどうか**があいまいさの有無を決める。

なお`int[] {1,1,1}`（`new`なし）はメソッド引数としては構文エラーになる。`{...}`の省略記法は**変数宣言の初期化時のみ**有効で、メソッド引数として渡すときは必ず`new int[] {...}`と書く必要がある（実機で`'.class'がありません`という構文エラーを確認済み）。

---

## 16. float/doubleの出力は必ず小数点付き、二項数値昇格は「一度混ざったら戻らない」

```java
int i = 7;      System.out.println(i);   // 7
float f = 7;    System.out.println(f);   // 7.0
double d = 7;   System.out.println(d);   // 7.0
long l = 7;     System.out.println(l);   // 7
```

`float`/`double`の`toString()`は、値が整数であっても**必ず小数点の前後に最低1桁ずつ数字を置く**という言語仕様がある。出力結果だけを見て「整数型か浮動小数点型か」を区別できるようにするための仕組み。

```java
float i = 7;
System.out.println(i + 2);          // 9.0（float + int → float）
System.out.println((int) (i + 2));  // 9（明示的にナローイングした場合のみ）
```

異なる数値型同士の演算は、自動的に「大きい方の型」に揃えてから計算される（二項数値昇格）。一度`float`/`double`が式に混ざると、結果の型もそこから`float`/`double`に固定され、**明示的にキャストしてナローイングしない限り、以降もずっと小数点付きの型のまま**になる。

---

## 17. `main`もただのメソッド：オーバーロード可・引数には型が必須

```java
public class Main {
    public static void main(Sample s) { s.val++; System.out.print(s.val); }
    public static int main(int i) { return i * i; }
    public static void main(String[] args) {   // ← JVMが呼ぶのはこれだけ
        Sample s = new Sample();
        main(s);
        int num = main(10);
        System.out.print(s.val + num);
    }
}
```

`main`という単語自体は予約語ではなく**ただの識別子**。JVMが特別扱いするのは`public static void main(String[] args)`という完全一致のシグネチャだけで、それ以外の`main`という名前のメソッド（`main(Sample s)`など）は、他の任意のメソッドと同様に**プログラム内から明示的に呼ばない限り実行されない**。インスタンスを引数に取ることも普通にできる（実機で`11111`を確認済み）。

```java
public static void main(s) { ... }   // NG：型を書かないと構文エラー
```

```
エラー: <identifier>がありません
```

JavaScriptと違い、**メソッドの引数には必ず型を明示する**必要がある（`var`も使用不可、既出）。理由は、Javaがコンパイル時にすべての型を確定させる静的型付け言語だから。オーバーロード解決（どの`main`を呼ぶか）も引数の型を手がかりに行われるため、型の明示は構文上必須かつオーバーロード機構の前提でもある。

---

## 18. `クラス名.メンバ`が使えるのはstatic専用（逆方向の確認）＋設計原則

9節の逆パターン。`static`が付いていないメソッドを`クラス名.メンバ`の形で呼ぶとエラーになる。

```java
public void reset() { count = 0; }   // staticなし = インスタンスメソッド
...
Counter.reset();   // NG
```

```
エラー: staticでないメソッド reset()をstaticコンテキストから参照することはできません
```

一方、**インスタンスメソッドの内部からstaticメンバーを使うことには一切制約がない**（禁止は常に「staticコンテキストからインスタンスメンバーへ」の一方通行）。

```java
public void printCount() {
    System.out.println(count);  // staticフィールドを無修飾で参照。問題なし
    Update();                    // staticメソッドを無修飾で呼ぶ。問題なし
}
```

またコンストラクタの判定条件（7.1）の実例として、`public void Counter(int count) { ... }`のように`void`が付いた同名メソッドは**コンストラクタとして扱われない**ため、そのクラスにはコンストラクタが実質1つも定義されていないことになり、コンパイラがデフォルトコンストラクタ（引数なし）を自動生成する（`new Counter()`が引数なしで動く理由）。

### 設計原則：「個々のインスタンスの値」と「全体の合計値」は別フィールドに分ける

`static`フィールド1つだけで「個々の値」と「全体の値」を同時に表現しようとすると、`c1.update(3)`のような呼び出しが実際には全インスタンス共通の値を書き換えてしまい、後から読んだときにどこで何が更新されているか分かりにくくなる。両方の概念が必要なら、最初からフィールドを分けるべき。

```java
private int myCount;             // ①インスタンスごとの値
private static int totalCount;   // ②全インスタンス共通の合計値

public void update(int c) {
    this.myCount += c;
    totalCount += c;
}
```

実機で検証済み：`c1.update(3)` → `c1.getMyCount()=3`、`c2.update(1)` → `c2.getMyCount()=1`、`BetterCounter.getTotalCount()=4`（両方の合計）。

---

## 疑問点・要復習（随時追記）

- IDE上で`setNum(int)`だけがエラー表示され`getNum()`はエラーにならなかった非対称性の正確な原因は未特定（javac上は問題なし。IDEの言語サーバー側のキャッシュ/解決順序の問題と推測されるが未検証）
