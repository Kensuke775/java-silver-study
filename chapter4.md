# 第4章 繰り返し文（forとwhile）

対応する教科書ページ: 175〜186ページ（章末問題1〜16）
学習環境: macOS（Apple Silicon）／ JDK 17（Homebrewの`openjdk@17`、17.0.20）
サンプル: `~/Downloads/sample/chap4/`

---

## 1. `while(false)`型の到達不能チェック（do-whileとの違い）

```java
do System.out.print("*");
while (false);              // OK

while (1 == 2)               // コンパイルエラー
    System.out.print("*");
```

- `do-while`は本体を**先に1回実行してから**条件を見る構文。条件が`false`でも本体は必ず1回は到達するので、到達不能にならずコンパイルが通る。
- 普通の`while`は**先に条件を見てから**本体に入る。条件がリテラル同士の演算などの**コンパイル時定数**で常に`false`と確定できる場合、本体に絶対到達できないと静的に証明できてしまい、コンパイルエラーになる（実際に検証済み、ex8/Main.javaで確認）。

**回避策**：条件を変数にする。

```java
int i = 1;
while (i == 2) { ... }   // コンパイルは通る（実行時にしか分からない値として扱われるため、チェック対象外）
```

**重要な補足**：この判定は「実行時に絶対入らないかどうか」ではなく、**コンパイル時定数式かどうか**で機械的に決まる。変数を使えば、実際には絶対trueにならない条件でも到達不能チェックの対象外になる。

---

## 2. 拡張for文の変数への再代入とスコープ

```java
for (int i : array) {
    i++;   // コンパイル可能（実害なし。次の周でarrayの値により上書きされる）
}
```

- 拡張for文の変数は、各周回ごとに新しく作られる**finalの付いていないローカル変数**（配列や参照先の値の**コピー**）。再代入しても元の配列は変わらない。
- 再代入できるかどうかは**型（int/Stringなど）ではなく`final`修飾子の有無**で決まる。`final`が付けば型を問わず再代入不可（コンパイルエラー）。
- `++`演算子は数値系（byte/short/char/int/long/float/double）専用で、`String`には使えない（別種のコンパイルエラー）。ただし`s = s + "x";`のような通常の再代入はStringでも合法で、intと同じく次周で上書きされる。

---

## 3. for文の3要素（初期化；条件；更新）はすべて省略可能

`;`2つの区切りだけは必須。省略した部分は自分で管理する必要がある。

```java
for (;;) { ... }              // 条件省略 = while(true)と同じ無限ループ（コンパイルエラーにならない）

int x = 10;
for (; x != 0; x--) { ... }   // 初期化省略：既にxが宣言済みなら可能

for (int x = 10; x != 0; ) {  // 更新省略：本体内で自分で増減する必要がある
    x--;
    System.out.print(x);
}
```

**更新省略時の落とし穴（実際に検証済み、元コード: `while(x!=0){print(x); x--;}` → `10987654321`）**

```java
// print → decrement の順（元と同じ順序）
for (int x = 10; x != 0; ) {
    System.out.print(x);
    x--;
}
// → 10987654321（元と一致）

// decrement → print の順（順序が逆）
for (int x = 10; x != 0; ) {
    x--;
    System.out.print(x);
}
// → 9876543210（開始が9、終了が0にズレる）
```

更新処理を本体のどこに置くか（printの前か後か）で結果が変わる。「更新を省略＝自分で書く」だけでなく、**書く位置**まで意識する必要がある。

**前置/後置デクリメントが同じ結果になるケース**：更新欄で`x--`と`--x`のどちらを書いても、単独の文として使われている（返り値をどこにも使っていない）場合は結果が同じ。前置/後置の違いが効くのは、`array[--count]`や`v += ++w + ...`のように**返り値を式の中で使っている場合のみ**。

**拡張for文の対象は配列かIterableのみ**：`for (int y : 10)`のように配列でも何でもない数値を書くとコンパイルエラー（`for-each not applicable to expression type`）。実行すらされない（1文字も出力されない）。

---

## 4. 変数のスコープ（ブロック内で宣言した変数は外から見えない）

```java
for (int i = 0; i < 2; ) {
    i++;
    int j;
    for (j = 0; j < i; j++) { }
    System.out.print(j + " ");
}
System.out.println(":" + i);   // コンパイルエラー：シンボルを見つけられません
```

`i`も`j`も外側forの`{}`内で宣言されているため、その`{}`を抜けた外側からはアクセスできない。

**重要**：コンパイルエラーが報告される行は、**宣言した行ではなく、スコープ外から使おうとした行**（上記の例なら`println`の行）。これはfinal変数への再代入エラーなど、他のコンパイルエラー全般にも共通する一般則。

同じ理屈がchap4/ex15（for文内で宣言した`count`をfor文の外の`println`で使おうとしてコンパイルエラー）にも当てはまる。

**nullは無関係**：`if (city == null)`のような比較はプリミティブ/参照型やスコープの話とは独立した論点。`==`によるnull比較は常に安全（NullPointerExceptionは起きない）。危険なのは`city.equals(null)`のように、**cityがnullのときにnullに対してメソッドを呼び出そうとする場合**（これは実行時のNullPointerExceptionであり、コンパイルエラーではない）。

---

## 5. switch内のbreak/continueの対象範囲（ifは完全に素通り）

```java
while (index < text.length()) {
    switch (text.charAt(index)) {
        case 'H': System.out.print(0);              // breakなし→フォールスルー
        case 'e': System.out.print(1); break;         // switchだけ抜ける
        case 'l': System.out.print(2); index++; continue;  // ループの次周へ
        case 'o': System.out.print(3);
    }
    index++;
}
```

| キーワード | 対象になる構造 |
|---|---|
| `break` | 自分を囲む一番近い**switchまたはループ** |
| `continue` | 自分を囲む一番近い**ループのみ**（switchは対象外、素通りする） |
| `if` | breakにもcontinueにも一切関係しない（完全に素通り） |

- `break`はswitch内では**switchを抜けるだけ**。ループの中にいればループ自体は続く。
- `continue`はswitchを無視して、外側のループに直接効く。switch以降の後続処理（上の例なら`index++`）も丸ごとスキップされる。
- **continueの着地点はループの種類で異なる**：
  - `while`/`do-while`：**条件判定**に戻る（本体に無条件で再突入するわけではない。条件がfalseならループはそこで終わる）
  - `for`：**更新式（`i++`など）を先に実行してから**条件判定に戻る（whileより1ステップ多い）

---

## 6. 多次元配列（ジャグ配列）

```java
int[][] array = new int[2][];
```

- `int[][]`という**型**の`[]`は「何次元か」を表す。`new int[2][]`の`[2]`は「その次元に何個の箱を作るか」を表す。
- サイズを指定しなかった次元（内側）は、箱として存在するが中身はまだ作られておらず**`null`**になる。`[[], []]`（空配列2つ）ではなく`[null, null]`（実際にArrays.deepToStringで検証済み）。
- 外側から順にサイズを確定させる必要があるため、`new int[][3]`（外を空にして内だけ指定）はコンパイルエラー。

```java
int[][] array = new int[2][];   // array = [null, null]
array[0] = new int[3];          // array[0]に実体ができる → [0, 0, 0]
array[1] = new int[]{1, 2};     // array[1]も実体ができる → [1, 2]
```

`array[0]`に代入する前にアクセスしようとすると、`null`に対する操作になるため`NullPointerException`が発生する。

---

## 疑問点・要復習（随時追記）

- （なし、現時点ですべて実機検証で解消済み）
