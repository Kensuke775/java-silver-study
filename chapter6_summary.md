# 6章 要点まとめ（問題6-1〜6-24）

散歩しながらでも読める、1問1行の早見表。詳しい議論・具体例は`chapter6_review.md`を参照。

| # | テーマ | 要点 | 正解 |
|---|---|---|---|
| 1 | クラス宣言の修飾子 | アクセス修飾子は重複不可。`sealed`は`class`より前に書く。単体`sealed`は同一ファイル内継承なら`permits`省略可 | A, C |
| 2 | フィールド隠蔽 vs オーバーライド | フィールドは**宣言型**基準（静的）、メソッドは**実体型**基準（動的）で解決される | B |
| 3 | protectedの公開範囲 | 同一パッケージなら、継承関係の有無を問わず全クラスからアクセス可能 | A |
| 4 | package-privateと別パッケージ継承 | デフォルトアクセスは、継承していても別パッケージからはアクセス不可（protectedとの違い） | D |
| 5 | オーバーロードの判定基準 | 名前＋引数リストだけで決まる。戻り値の型は無関係。引数が同じで戻り値が非互換だとコンパイルエラー | B, D |
| 6 | privateメソッドと動的束縛 | privateは継承されない＝オーバーライド不成立＝動的束縛の対象外。宣言型を見て静的に解決 | B |
| 7 | コンストラクタ連鎖 | 実行順は必ず親から子。`super()`を書かなければ暗黙的に親の引数なしコンストラクタが呼ばれる | D |
| 8 | コンストラクタ呼び出しは先頭で1回だけ | `super(...)`か`this(...)`のどちらか一方を、コンストラクタの最初の行でのみ書ける | A |
| 9 | recordの暗黙extends | 全recordは暗黙的に`java.lang.Record`を継承。明示的な`extends Record`は構文エラー | A, F |
| 10 | コンパクトコンストラクタ | 引数リストなし・`this.`不要で残りは暗黙初期化。標準コンストラクタは`this.`必須 | B, E |
| 11 | recordの不変性 | コンポーネントフィールドは`private final`。外部からの再代入は不可 | C |
| 12 | クラス/インタフェースの基本ルール | 抽象メソッドを持つクラスは抽象クラス必須。recordは他クラスをextends不可。interfaceは多重継承可 | A, C, D |
| 13 | 抽象メソッドのオーバーライドとアクセス修飾子 | interface由来は暗黙public（明示必須）。抽象クラス自身の抽象メソッドは宣言通りでよい | B, E |
| 14 | 戻り値非互換なdefaultメソッドの衝突 | 両立するオーバーライドが書けない場合、実装するインタフェースを1つに絞るしかない | A |
| 15 | interfaceのstaticメンバ | static変数はインスタンス経由でもOK。staticメソッドはインタフェース名経由必須 | B, C |
| 16 | sealedのpermits整合性 | 実際に継承しているクラスがpermitsから漏れているとコンパイルエラー | A, C |
| 17 | instanceofと継承チェーン | instanceofは縦の関係（祖先・実装インタフェース）だけを見る。共通の親を持つ兄弟同士は無関係 | D |
| 18 | ダウンキャストの危険性 | コンパイルは通っても、実体が違えば実行時にClassCastException。instanceofで事前確認が安全 | D |
| 19 | ArrayList.set()の範囲 | `set()`は既存インデックス限定（`add()`と違い末尾への拡張は不可）。範囲外はIndexOutOfBoundsException | E |
| 20 | ArrayList.set()の戻り値 | 戻り値はboolean**ではなく**、置き換え「前」にあった要素そのもの | B |
| 21 | remove()のオーバーロード | `remove(int)`＝インデックス指定・戻り値は削除要素。`remove(Object)`＝値指定・戻り値はboolean。int値は前者が優先される | C |
| 22 | Mapの追加メソッド | Mapに`add()`は存在しない。追加は`put(key, value)` | E |
| 23 | ジェネリクスの共変性とダイヤモンド演算子 | ジェネリクスは配列と違い**不変**（`List<Object>`≠`List<String>`）。`<>`省略は右辺のみ | A, C, D, E |
| 24 | Arrays.mismatch() | 各要素を`equals()`で比較し、最初に不一致となるインデックスを返す（全一致なら-1） | D |

---

## テーマで串刺しにすると

- **「宣言型 vs 実体」がすべての土台**：フィールド(#2)、privateメソッド(#6)、キャスト(#18)は宣言型基準。publicメソッドのオーバーライド(#2,#7)、instanceof(#17)は実体基準
- **オーバーライドの成立条件**：シグネチャ一致＋戻り値が共変＋アクセス範囲を狭めない（#5, #13, #14）
- **sealed/permitsは「直接の子」かつ「final/sealed/non-sealedのいずれか必須」**（#12, #16）
- **コレクション操作はオーバーロードの罠が多い**：add/set/removeで引数の型により挙動が変わる（#19〜#22）
